extends base
include mixins/projectForm

block title
  title Nuevo Proyecto

block content
  .contenido-principal
    h1= mainTitle

    if notErrors
      .alerta.correcto= notErrors

    if errors
      each error in errors
        .alerta.error= error.message
    +projectForm(project)
  
    